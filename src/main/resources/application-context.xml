<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="classpath:musicPlayer.properties"/>

    <!--     for scope="prototype" - init call each time when create bean but destroy did not call-->
    <bean id="classicalBean"
          class="ru.cherniak.springlearn.ClassicalMusic"
    init-method="doMyInit"
    destroy-method="doMyDestroy">
    </bean>

<!--    factory method in the RockMusic-->
    <!--   if scope singleton - call factory-method only onetime -->
    <bean id="rockBean"
          class="ru.cherniak.springlearn.RockMusic"
          scope="prototype"
    factory-method="getRockMusic">
    </bean>
    <bean id="rapBean"
          class="ru.cherniak.springlearn.RapMusic">
    </bean>


    <bean id="musicPlayer"
          class="ru.cherniak.springlearn.MusicPlayer"
          scope="prototype">
        <property name="musicList">
            <list>
                <!--       <list value-type="ru.cherniak.springlearn.Music">-->
                <ref bean="classicalBean"/>
                <ref bean="rockBean"/>
                <ref bean="rapBean"/>
            </list>
        </property>
        <!--        <property name="music" ref="musicBean"/>-->

        <!--        <property name="name" value="Player name"/>-->
        <!--        <property name="volume" value="50"/>-->
        <!--this with used musicPlayer.properties = -->
        <property name="name" value="${musicPlayer.name}"/>
        <property name="volume" value="${musicPlayer.volume}"/>

    </bean>

</beans>